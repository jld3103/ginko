name: server
description: The Ginko server
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  http: ^0.12.0+2
  html: ^0.14.0+2
  intl: ^0.15.8
  crypto: ^2.0.6
  dio: ^2.1.7
  cookie_jar: ^1.0.0
  models:
    path: ../models