name: server
description: The Ginko server
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  http: ^0.12.0+2
  html: ^0.14.0+2
  intl: ^0.15.8
  crypto: ^2.0.6
  models: any

dev_dependencies:
  pedantic: ^1.7.0

dependency_overrides:
  models:
    path: ../models