name: tests
description: The Ginko tests
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  sliding_up_panel: ^0.3.4
  models:
    path: ../models
  server:
    path: ../server
  app:
    path: ../app
  translations:
    path: ../translations

dev_dependencies:
  test: ^1.6.3
  flutter_test:
    sdk: flutter